# EDIS Runtime Collector 1.6.4

This qualification-hardening patch preserves Runtime Package and Snapshot Schema `1.4.0` and adds no permissions.

- Prevents text preview from inheriting text belonging to descendant form controls or editable regions.
- Serializes capture finalization with an atomic, expiring IndexedDB claim to prevent duplicate assembly during polling, completion messages, or worker recovery.
- Aligns browser E2E requests and semantic replay comparisons with the production protocol and canonicalization implementation.
- Strengthens deterministic test execution and browser qualification evidence.
- Keeps WordPress 7 visibility coverage explicitly labeled as a controlled synthetic fixture, not a real-site certification.
