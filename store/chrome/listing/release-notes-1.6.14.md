# EDIS Runtime Collector 1.6.14

- Bounds text-shape traversal to selected evidence subtrees and deterministic global budgets.
- Replaces quadratic sibling identity work with capture-scoped structural indexes.
- Adds cumulative session evidence accounting and fail-fast export capacity checks.
- Reduces snapshot, screenshot, hashing, ZIP parsing, and diagnostic-sorting allocations.
- Replaces sequential screenshot reads with indexed bulk retrieval and moves maintenance off the capture hot path.
- Makes viewport-image readiness mutation-aware and sends lightweight session summaries to extension UIs.
- Adds performance regression coverage for large text trees, sibling sets, ZIP views, IndexedDB accounting, and incremental image readiness.

No new permission, remote service, evidence schema, tracking behavior, or network feature was added.
