# Supported languages

- English (`en`) — default locale.
- Persian (`fa`) — right-to-left interface.

Store listing translations must be entered and reviewed separately in the dashboard. Runtime localization files are included in the package.
