# Local-only statement

Capture, validation, persistence, checksum generation, screenshot handling, and ZIP creation occur inside the browser extension. The production extension contains no network client and does not upload evidence.

Users decide when to capture and when to export. Exported files remain local unless the user moves or shares them outside the extension.
