# EDIS Browser Runtime Evidence Collector 1.6.20

Release date: 2026-07-31

## Correctness closure

- Unifies the computed-style collection/validation limit at 2048 characters and records bounded omissions as partial evidence.
- Corrects effective visibility so descendant `visibility:visible` can override an ancestor `visibility:hidden` without disabling irreversible subtree pruning.
- Fails closed when capture readiness enters `ERROR`, including Minimum Python Feed finalization.
- Uses one WordPress Admin Bar classifier that requires WordPress-specific markers and visible `#wpadminbar` evidence for `PRESENT`.
- Counts only effectively visible modal candidates and re-samples viewport-image membership at final readiness samples.
- Makes export dispatch non-destructive; persisted evidence is removed only through the explicit clear-data flow. Preferences migrate to schema 5.
- Enforces exact Runtime Evidence ZIP and checksum inventories.
- Requires full exact Chrome Stable + Microsoft Edge Stable qualification for trusted release packaging.
- Binds source packaging and personal release packaging to the committed source manifest, current release evidence, exact Git Head, and version 1.6.20.

Runtime Snapshot schema remains `1.6.0`.
