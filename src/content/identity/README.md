# Identity module

Element identity is implemented in the browser-independent domain module and consumed by the transient content collector. Real Elementor IDs take precedence over stable DOM references; random identifiers are never generated.
