# Manifest Sources

Chrome and Edge manifests use a Manifest V3 module service worker and the Side Panel API. The Firefox manifest is experimental and uses a non-persistent background script plus sidebar fallback. Build output selects a manifest without changing Domain schemas.
