# Manifest Sources

Chrome and Microsoft Edge manifests use Manifest V3, a module service worker, temporary `activeTab` access, `chrome.scripting`, extension-scoped storage, and the Side Panel API. Neither manifest declares persistent host permissions or remote code.
