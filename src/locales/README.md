# Localization source

Runtime locale catalogs are stored in the browser-required `_locales` directory. English is the default locale and Persian includes RTL support. UI code uses browser i18n lookup and never inserts localized text as HTML.
